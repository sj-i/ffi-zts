; php.ini fragment fed to the embedded ZTS interpreter.
; extension_dir points at the FFI-linked parallel.so build so that
; parallel's undefined references resolve into the libphp.so we loaded
; via FFI instead of into the host NTS binary.
extension_dir=/home/user/php-zts/lib/php/extensions/ffi-zts
extension=parallel.so
